# どこに店ができるかは当てられる。何の店かは当てられない

全国471,024個の500mメッシュ（1億2,532万人）で、小売・飲食・サービス30業種の立地を測った記録。
「何人いればその地域で商売が成り立つか」を業界の目安ではなく実データから推定するところから始めて、
当てられることと当てられないことの境界を引くまで。

すべて公的統計と再配布可能なデータで、スクレイピングは使っていない。

---

## 結論

**1. まだ1店も無いマスの順位づけは当たる（7.5倍）**

実際に開いた店を、モデルが上位10%に選んだマスがどれだけ捕まえたか。
でたらめなら10%のところを75.1%捕まえた。

| 業態 | 検証に使った新店 | 倍率 | 95%信頼区間 |
|---|---|---|---|
| コンビニ | 1,236 | **7.5倍** | 7.3 〜 7.7 |
| ドラッグ・薬局 | 129 | 8.4倍 | 7.6 〜 8.9 |
| スーパー | 156 | 8.3倍 | 7.6 〜 8.8 |

ただし絶対値は小さい。上位10%に選ばれたマスでも、1年で店ができる確率は2.17%（全体は0.29%）。
「ここに店ができる」ではなく「ここは平均の7.5倍できやすい」という道具。

**2. モデルは全国1つで足りる**

福岡県だけで学習した係数を他県にそのまま当てて、現地で学習した場合との差は最大 +0.031。
秋田県では福岡で学習したほうが良かった（標本が大きいため）。県を1つずつ隠す46回の検証でも
倍率の中央値7.2倍。**地域ごとにモデルを作る必要は無い**。

面白いのは、係数は県で動くのに予測は動かないこと。`log人口` の係数は
福岡0.230 / 佐賀0.576 / 長崎0.111 と3倍違うが、特徴量どうしが相関しているので
出てくる予測は一致する。係数を読んで意味を語るなら県ごとに見るべきだが、当てるだけなら1つでよい。

**3. 「この地域には何の店ができるか」は当てられない**

同じモデルでも問いの向きで結果が変わる。10業種・47県・74,507件の開店データで3通り測って、
いずれも信号が無かった。

| 測り方 | 結果 |
|---|---|
| 県単位・二重中心化（県の差と業種の差を抜く） | r = +0.085（t = +1.5） |
| メッシュ単位・業種ごとのAUC | 平均 0.508 |

理由ははっきりしている。**特徴量に業種を分ける情報が無い**。
人口・従業者・道路・駅・土地利用は、どの業種にも同じ向きに効く。
コンビニに良いマスはドラッグにもレストランにも良い。

**4. 効いたのは従業者数だけだった**

思いつく候補を順に足したが、動いたのは1つだけ（市街地に絞ったAUCの差）。

| 足した情報 | コンビニ | ドラッグ | スーパー |
|---|---|---|---|
| 年齢構成 | +0.006 | ±0.000 | −0.001 |
| 他業態の集積 | +0.004 | +0.015 | +0.008 |
| 地価（1,840地点を内挿） | +0.006 | +0.005 | +0.009 |
| 建物数 | +0.001 | −0.001 | +0.001 |
| **従業者数** | **+0.035** | **+0.039** | **+0.057** |

しかも従業者数を入れると、地価も他業態の集積も効かなくなる。どちらも昼間の需要の代理だった。
駅の乗降客数（全国10,482駅）も同じく吸収される。

**年齢は全業種で効かない。**保育園（0-4歳）・学習塾（5-19歳）・病院（高齢）でも動かない。
年齢別人口は総人口と対数で r=0.98〜0.99 あり、独立な情報を持っていないため。
500m四方では年齢構成のばらつき（変動係数0.32）が人口そのもののばらつき（0.97）に対して小さすぎる。

**5. 集積の正体はドミナント出店だった**

Foursquareの `website` のドメインでチェーンを識別できる。新規開店2,148件を屋号で分け、
周辺3×3の「コンビニのうち自社が占める割合」で自社の新店を他社の新店と見分けられるかを測った。

| チェーン | AUC | 全国シェア |
|---|---|---|
| ミニストップ | **0.934** | 2.9% |
| ファミリーマート | 0.706 | 24.5% |
| ローソン | 0.694 | 21.8% |
| セブン-イレブン | 0.569 | 33.2% |

全チェーンが自社の近くに出している。全国シェアが小さいチェーンほどはっきり出る。

**6. チェーンと個店は反発する**

同じ業種でも、開店を言い当てるのは正反対のほうだった（市街地AUC）。

| 業種 | 個店比率 | チェーン密度 | 個店密度 |
|---|---|---|---|
| コンビニ | 11.5% | **0.540** | 0.429 |
| パン屋 | 93.6% | 0.379 | **0.577** |
| 居酒屋・バー | 95.6% | 0.322 | **0.631** |

負けた側が0.5を**下回る**のが肝。新しい個店の居酒屋はチェーン居酒屋のある場所を避け、
新しいコンビニは個店コンビニのある場所を避けている。2つの世界は互いに反発している。

**7. 個店の集積は土地の性質では説明できない**

門前町・城下町・港町・温泉街・アーケード商店街——どれも「昔からそこに人が集まる理由があった場所」だが、
説明力はほとんど無かった。残差にはポアソンのばらつきが混ざるので、
同じ見込みから作った乱数と分散を比べて「そもそも説明できる分」で割り直した値。

| 業種 | 説明できる分 | 地形･歴史＋用途地域 | 隣のマス |
|---|---|---|---|
| 居酒屋・バー | 52% | 10% | 18% |
| 美容・理容 | 32% | 13% | **31%** |
| カフェ | 29% | 14% | **31%** |

土地の性質より「隣に何があるか」のほうが2倍以上説明力が大きく、それでも合計3割前後。
**その場所に先に誰かが店を出したという事実そのものが最大の説明変数**だった。

**8. 1店あたり人口は全国でほぼ同じ**

| 沖縄 | 愛知 | 秋田 | 佐賀 | 長崎 | 福岡 | 宮崎 |
|---|---|---|---|---|---|---|
| 1,613 | 1,854 | 1,867 | 1,912 | 2,010 | 2,033 | 2,055 |

人口密度も車依存度も産業構造もまるで違う県で1.3倍の幅に収まる。
閉店した711店から測った閾値（周辺の1店あたり人口が2,200人を割ると閉店率が跳ね上がる）は、
特定の地域の事情ではなさそう。

---

## できないこと

正直に書いておく。

- **閉店は予測できない**（AUC 0.61）。しかも多変量が単変量に負ける。
  さらに Foursquare の `date_closed` は現実の閉店を測っていない——
  人口が減る県ほど閉店が多いはずなのに相関が **+0.44** と符号が逆で、
  閉店年が2017-18年に山を作る（愛知の閉店の68.5%）。データ側の一括整理とみられる
- **大都市圏のコンビニは当たらない**。周辺人口密度で 5.7倍（500人/km²未満）から
  2.3倍（6,000人/km²以上）まで単調に落ちる。密集地だけで学習し直しても直らない
  （情報の側の問題で、密集地の空きマスは条件がどれも似ている）。
  ただし**スーパーは都心でも4.7倍を保つ**ので、一律に大都市圏を外すのは誤り
- **飲食では素朴な対抗馬に勝てない**。「既存店舗数で並べるだけ」と互角。
  飲食の新店は85.5%が既に店のある場所に出るので、何で並べても当たるため
- **7.5倍という数字には「都市と田舎を分ける」ぶんが入っている**。
  密集度をそろえて測ると2.5〜4.6倍。1つの街の中で場所を選ぶ用途ならこちらが実力

---

## 測り方

**物差し**は「まだ1店も無いマスの中での順位づけ」に定めた。
「どこに店があるか」は既存店舗数で並べるだけで当たるのでモデルの仕事ではない。
まだ1店も無いマスでは対抗馬が全マス0で並ぶため原理的に順位をつけられず、そこがモデルの持ち場になる。

全メッシュのAUCは「田んぼの真ん中に店は無い」という自明な当たりで水増しされる（0.87）。
判断が要る市街地（周辺人口3,000人以上）に絞ると 0.77〜0.82 に落ちる。以後こちらを使った。

**検証**はすべて空間ホールドアウト（県を丸ごと隠して学習）と、
実際の新規開店による前向き検証（新店を差し引いてから学習し直した汚染ゼロの条件）。

**倍率を出すときは必ず素朴な対抗馬を並記する**。でたらめ（1.0倍）は対抗馬ではない。
「今すでにそこにある量」で並べたものが最低限の対抗馬になる。

---

## データ源

すべて再配布可能・スクレイピング不要。

| 何 | 出所 |
|---|---|
| 店舗 | Foursquare OS Places（Apache 2.0）。`date_closed` を持つので営業中で絞れる |
| 人口 | 国土数値情報 500mメッシュ別将来推計人口（H30国政局推計） |
| 従業者数 | e-Stat 統計GIS 経済センサス活動調査 500mメッシュ（統計表 T000918） |
| 土地利用 | 国土数値情報 L03-b-u（100mメッシュ詳細版） |
| 用途地域 | 国土数値情報 A29 |
| 道路・駅・地形の目印 | Geofabrik の地域抽出（OpenStreetMap） |
| 駅の乗降客数 | 国土数値情報 S12 |
| 地価 | 国土数値情報 L01 地価公示 ＋ L02 都道府県地価調査 |
| 昼夜間人口比 | 統計ダッシュボード API（キー不要） |
| 開店（小売） | 国税庁 酒類販売業免許の新規取得者名等一覧（全国12国税局・47県） |
| 開店（飲食） | 厚生労働省 食品衛生申請等システム オープンデータ（157自治体） |
| 開店（医療） | 厚生局 コード内容別医療機関一覧表（指定年月日） |
| 地名 | 国土地理院 逆ジオコーディング |

**消えるデータがある。**酒販免許は国税局ごとに8〜12か月ぶんしか残らない
（金沢局は9か月、大阪局は8か月）。食品衛生申請等システムは上書き更新される。
どちらも取り逃したら二度と手に入らないので、毎月取得する仕組みを回して版を積んでいる。

---

## 再現

```bash
python3 -m venv .venv && .venv/bin/pip install numpy scipy duckdb pdfplumber openpyxl osmium

python3 fetch_population.py all      # 1,741市町村
python3 fetch_context.py             # 昼夜間人口比ほか
python3 build_pref.py all            # 471,024メッシュの基盤
python3 add_landuse.py               # 土地利用
python3 add_workers.py               # 従業者数
python3 add_roads.py                 # 道路と駅（OSMの地域抽出が要る）
python3 fetch_foursquare.py && python3 build_mesh_fsq.py   # 店舗
python3 fetch_formats.py             # 30業種
python3 add_zoning.py                # 用途地域
python3 measure_national.py          # 検証
```

集計済みのメッシュデータは `public/mesh.csv.gz`（471,024行 × 104列・23MB）。
列の説明は `public/COLUMNS.md`。

---

## 主なスクリプト

| ファイル | 何をするか |
|---|---|
| `mesh_model.py` | ポアソン回帰と特徴量。`TARGET` を変えれば業種が変わる |
| `measure_national.py` | 全国の前向き検証と、県を1つずつ隠す移転テスト |
| `baseline.py` | 素朴な対抗馬との比較。物差しを決めた根拠 |
| `ablation.py` | 特徴量の総当たり |
| `simulate.py` | まだ1店も無いマスを見込み順に並べる |
| `test_which_format_mesh.py` | 「何の店ができるか」の検定 |
| `fetch_chains.py` / `fetch_chain_split.py` | チェーンと個店の分離 |
| `add_historic.py` | 地形・歴史の目印 |
| `formats.py` | 業種の一覧。1行足せば業種が増える |
| `prefs.py` | 47都道府県の対応表 |
| `region.py` | 対象地域。緯度経度の定数を1か所に集めてある |

---

## 引用

`CITATION.cff` を参照。

---

## このリポジトリと公開リポジトリの関係

- **ここ**（`~/dev/01_projects/shoken-model`）は作業用。生データ（酒販免許PDF・
  食品衛生の版・国土数値情報・OSM抽出、あわせて約5GB）と全履歴を持つ
- **公開**は `github.com/allfestaboss/shoken-model`。コードと派生データだけで24MB。
  ローカルの複製は `~/dev/01_projects/shoken-model-public`

**このリポジトリが正本。**公開側は `./publish.sh` で毎回ここから作り直す。
手で写すと片方が腐るので写さない。

```bash
./publish.sh          # public/ を作り直し、公開リポジトリに反映して差分を見せる
```

履歴からは再取得できる生データ（国土数値情報・OSM抽出・e-Stat、約6.4GB）を
`git filter-repo` で落としてある（.git 591MB → 145MB、コミット37本は全部保持）。
残しているのは**二度と取れないもの**だけ:

| | 大きさ | なぜ残すか |
|---|---|---|
| `data/nta` | 47MB | 酒販免許。国税局ごとに8〜12か月で消える |
| `data/food` | 82MB | 食品衛生の版。上書き更新される |
| `data/medical` | 15MB | 厚生局の一覧 |
| `data/*_geo.csv` | 0.6MB | 座標化の結果。作り直すと数時間かかる |
